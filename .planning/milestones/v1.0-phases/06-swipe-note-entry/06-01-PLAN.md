---
phase: 06-swipe-note-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/logStore.ts
  - src/components/joystick/useSwipeLog.ts
  - src/components/joystick/NoteEntryModal.tsx
  - src/components/joystick/Joystick.tsx
  - src/components/joystick/index.ts
  - src/components/joystick/NoteEntryModal.test.tsx
  - src/components/ui/LogHistoryItem.test.tsx
autonomous: true
requirements: [LOG-04]
must_haves:
  truths:
    - "After a swipe, a bottom-sheet modal appears offering optional note entry"
    - "User can type a note and save it to the log entry"
    - "User can skip the note modal with zero friction (tap Skip, overlay, or back)"
    - "addLog returns the new log ID so the modal knows which log to annotate"
  artifacts:
    - path: "src/stores/logStore.ts"
      provides: "addLog returns Promise<string> instead of Promise<void>"
      contains: "return id"
    - path: "src/components/joystick/useSwipeLog.ts"
      provides: "pendingLogId state and clearPendingLogId callback"
      contains: "pendingLogId"
    - path: "src/components/joystick/NoteEntryModal.tsx"
      provides: "Bottom-sheet modal with TextInput for note entry"
      min_lines: 60
    - path: "src/components/joystick/Joystick.tsx"
      provides: "NoteEntryModal wired into Joystick render tree"
      contains: "NoteEntryModal"
    - path: "src/components/joystick/NoteEntryModal.test.tsx"
      provides: "Wave 0 test stub for NoteEntryModal"
      contains: "NoteEntryModal"
    - path: "src/components/ui/LogHistoryItem.test.tsx"
      provides: "Wave 0 test stub for LogHistoryItem"
      contains: "LogHistoryItem"
  key_links:
    - from: "src/components/joystick/useSwipeLog.ts"
      to: "src/stores/logStore.ts"
      via: "addLog returns log ID, stored as pendingLogId"
      pattern: "const logId = await addLog"
    - from: "src/components/joystick/NoteEntryModal.tsx"
      to: "src/stores/logStore.ts"
      via: "updateLogNote called with pendingLogId and note text"
      pattern: "updateLogNote\\(logId"
    - from: "src/components/joystick/Joystick.tsx"
      to: "src/components/joystick/NoteEntryModal.tsx"
      via: "NoteEntryModal rendered with pendingLogId from useSwipeLog"
      pattern: "<NoteEntryModal"
---

<objective>
Wire the post-swipe note entry flow: modify addLog to return the log ID, update useSwipeLog to expose a pendingLogId state, create a NoteEntryModal bottom-sheet component, and integrate it into the Joystick component. Also creates Wave 0 test stubs required by VALIDATION.md for NoteEntryModal and LogHistoryItem.

Purpose: Satisfies LOG-04 (optional text notes on log entries) -- the note ENTRY path.
Output: Working note entry modal that appears after every swipe, allowing optional note attachment. Test stubs ready for execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-swipe-note-entry/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/database/types.ts:
```typescript
export interface Log {
  id: string;
  pillarId: number;
  direction: SwipeDirectionType;
  targetId: string | null;
  note: string | null;
  createdAt: string;
}
```

From src/stores/logStore.ts (CURRENT — will be modified):
```typescript
interface LogState {
  addLog: (pillarId: number, direction: SwipeDirectionType, targetId?: string | null, note?: string | null) => Promise<void>;
  updateLogNote: (id: string, note: string) => Promise<void>;
  todayLogs: Log[];
}
```

From src/components/joystick/useSwipeLog.ts (CURRENT — will be modified):
```typescript
export function useSwipeLog(pillarId: PillarId) {
  // returns { handleSwipe }
}
```

From src/components/joystick/types.ts:
```typescript
export interface SwipeResult {
  pillarId: PillarId;
  direction: SwipeDirection;
  wasHeld: boolean;
}
```

From src/constants/colors.ts:
```typescript
export const colors = {
  background: '#0A0A0F',
  surface: '#14141F',
  surfaceLight: '#1E1E2E',
  border: '#2A2A3A',
  textPrimary: '#F0F0F5',
  textSecondary: '#8888A0',
  textMuted: '#555570',
  accent: '#F5A623',
  overlay: 'rgba(0, 0, 0, 0.6)',
};
```

From src/constants/theme.ts:
```typescript
export const typography = {
  fontFamily: { regular: 'Inter_400Regular', medium: 'Inter_500Medium', semibold: 'Inter_600SemiBold', bold: 'Inter_700Bold' },
  sizes: { xs: 10, sm: 12, md: 14, lg: 16, xl: 20, xxl: 28, hero: 36 },
};
export const spacing = { xs: 4, sm: 8, md: 12, lg: 16, xl: 24, xxl: 32, xxxl: 48 };
```

From src/components/goals/TargetFormModal.tsx (bottom-sheet modal pattern):
```typescript
// Established pattern: Modal + transparent + animationType="slide" + KeyboardAvoidingView
<Modal visible={visible} animationType="slide" transparent onRequestClose={onClose}>
  <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'} style={styles.overlay}>
    <View style={styles.card}>...</View>
  </KeyboardAvoidingView>
</Modal>
```

From src/components/joystick/Joystick.tsx (current handleSwipe usage):
```typescript
const { handleSwipe } = useSwipeLog(pillarId);
// handleSwipe is called in handleSwipeComplete and handleHoldEnd callbacks
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs for NoteEntryModal and LogHistoryItem</name>
  <files>src/components/joystick/NoteEntryModal.test.tsx, src/components/ui/LogHistoryItem.test.tsx</files>
  <action>
Create minimal passing test stubs required by VALIDATION.md. These are placeholder files that will be expanded with real tests once the components are implemented in later tasks. They must exist so the Nyquist automated verify commands have files to reference.

**File 1: src/components/joystick/NoteEntryModal.test.tsx (NEW)**

```typescript
/**
 * Wave 0 test stub for NoteEntryModal.
 * Expanded with real tests after component is created in Task 2.
 */
import React from 'react';

describe('NoteEntryModal', () => {
  it('stub: component module will be testable', () => {
    // Wave 0 placeholder — real tests added after NoteEntryModal.tsx is created
    expect(true).toBe(true);
  });
});
```

**File 2: src/components/ui/LogHistoryItem.test.tsx (NEW)**

```typescript
/**
 * Wave 0 test stub for LogHistoryItem.
 * Expanded with real tests after component is created in Plan 06-02.
 */
import React from 'react';

describe('LogHistoryItem', () => {
  it('stub: component module will be testable', () => {
    // Wave 0 placeholder — real tests added after LogHistoryItem.tsx is created
    expect(true).toBe(true);
  });
});
```
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/life && test -f src/components/joystick/NoteEntryModal.test.tsx && test -f src/components/ui/LogHistoryItem.test.tsx && echo "STUBS EXIST"</automated>
  </verify>
  <done>Both Wave 0 test stub files exist at the paths declared in VALIDATION.md. They contain minimal passing tests that will be expanded after the components are implemented.</done>
</task>

<task type="auto">
  <name>Task 1: Modify addLog return type and update useSwipeLog with pendingLogId state</name>
  <files>src/stores/logStore.ts, src/components/joystick/useSwipeLog.ts</files>
  <read_first>
    - src/stores/logStore.ts
    - src/components/joystick/useSwipeLog.ts
    - src/components/joystick/types.ts
    - src/components/joystick/constants.ts
  </read_first>
  <action>
**File 1: src/stores/logStore.ts**

Two changes to make:

1. Change the `addLog` type signature in `LogState` interface from `Promise<void>` to `Promise<string>`:
```typescript
addLog: (
  pillarId: number,
  direction: SwipeDirectionType,
  targetId?: string | null,
  note?: string | null
) => Promise<string>;
```

2. Add `return id;` as the last line of the `addLog` implementation (after the `set()` call on line 61):
```typescript
set((state) => ({
  todayLogs: [newLog, ...state.todayLogs],
}));

return id;  // Return the generated UUID for post-swipe note modal
```

No other changes to logStore.ts. The `updateLogNote` method already exists and works correctly.

**File 2: src/components/joystick/useSwipeLog.ts**

Rewrite the hook to:
1. Import `useState` from react (add to existing `useRef, useCallback` import)
2. Add `pendingLogId` state: `const [pendingLogId, setPendingLogId] = useState<string | null>(null);`
3. Capture the return value of `addLog`: `const logId = await addLog(result.pillarId, result.direction, targetId);`
4. After haptic feedback, set pending log ID with a 50ms delay (prevents gesture handler conflict per research pitfall 2): `setTimeout(() => setPendingLogId(logId), 50);`
5. Add `clearPendingLogId` callback: `const clearPendingLogId = useCallback(() => setPendingLogId(null), []);`
6. Return `{ handleSwipe, pendingLogId, clearPendingLogId }` instead of just `{ handleSwipe }`

Complete updated file:
```typescript
import { useRef, useCallback, useState } from 'react';
import * as Haptics from 'expo-haptics';
import { useLogStore } from '../../stores/logStore';
import type { PillarId } from '../../constants/pillars';
import type { SwipeResult } from './types';
import { DEBOUNCE_MS } from './constants';

export function useSwipeLog(pillarId: PillarId) {
  const addLog = useLogStore((state) => state.addLog);
  const [pendingLogId, setPendingLogId] = useState<string | null>(null);
  const lastSwipeTime = useRef<number>(0);

  const handleSwipe = useCallback(async (result: SwipeResult, targetId: string | null = null) => {
    const now = Date.now();
    if (now - lastSwipeTime.current < DEBOUNCE_MS) {
      return;
    }
    lastSwipeTime.current = now;

    const logId = await addLog(result.pillarId, result.direction, targetId);

    if (targetId) {
      Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
    } else {
      Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
    }

    // Small delay prevents gesture handler conflict with modal autoFocus
    setTimeout(() => setPendingLogId(logId), 50);
  }, [addLog]);

  const clearPendingLogId = useCallback(() => setPendingLogId(null), []);

  return { handleSwipe, pendingLogId, clearPendingLogId };
}
```
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/life && grep -n "return id" src/stores/logStore.ts && grep -n "Promise<string>" src/stores/logStore.ts && grep -n "pendingLogId" src/components/joystick/useSwipeLog.ts && grep -n "clearPendingLogId" src/components/joystick/useSwipeLog.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/logStore.ts contains `=> Promise<string>` in the addLog type signature
    - src/stores/logStore.ts contains `return id;` after the set() call in addLog implementation
    - src/components/joystick/useSwipeLog.ts contains `const [pendingLogId, setPendingLogId] = useState<string | null>(null)`
    - src/components/joystick/useSwipeLog.ts contains `const logId = await addLog(`
    - src/components/joystick/useSwipeLog.ts contains `setTimeout(() => setPendingLogId(logId), 50)`
    - src/components/joystick/useSwipeLog.ts contains `clearPendingLogId`
    - src/components/joystick/useSwipeLog.ts returns `{ handleSwipe, pendingLogId, clearPendingLogId }`
    - src/components/joystick/useSwipeLog.ts imports `useState` from react
  </acceptance_criteria>
  <done>addLog returns the new log UUID as Promise&lt;string&gt;. useSwipeLog exposes pendingLogId (set after each swipe) and clearPendingLogId (to dismiss the note modal).</done>
</task>

<task type="auto">
  <name>Task 2: Create NoteEntryModal and wire into Joystick component</name>
  <files>src/components/joystick/NoteEntryModal.tsx, src/components/joystick/Joystick.tsx, src/components/joystick/index.ts</files>
  <read_first>
    - src/components/joystick/Joystick.tsx
    - src/components/goals/TargetFormModal.tsx
    - src/components/joystick/index.ts
    - src/constants/colors.ts
    - src/constants/theme.ts
    - src/stores/logStore.ts
  </read_first>
  <action>
**File 1: src/components/joystick/NoteEntryModal.tsx (NEW)**

Create a bottom-sheet modal following the exact pattern from TargetFormModal. Props interface:
```typescript
interface NoteEntryModalProps {
  logId: string | null;   // null = hidden
  onClose: () => void;    // called after save or skip
}
```

Full implementation:

```typescript
import React, { useState, useEffect } from 'react';
import {
  Modal,
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  KeyboardAvoidingView,
  Platform,
} from 'react-native';
import { useLogStore } from '../../stores/logStore';
import { colors } from '../../constants/colors';
import { spacing, typography } from '../../constants/theme';

interface NoteEntryModalProps {
  logId: string | null;
  onClose: () => void;
}

export function NoteEntryModal({ logId, onClose }: NoteEntryModalProps) {
  const updateLogNote = useLogStore((state) => state.updateLogNote);
  const [note, setNote] = useState('');

  // Reset note text when modal opens with a new logId
  useEffect(() => {
    if (logId) {
      setNote('');
    }
  }, [logId]);

  const handleSave = async () => {
    if (logId && note.trim()) {
      await updateLogNote(logId, note.trim());
    }
    onClose();
  };

  const handleSkip = () => {
    onClose();
  };

  return (
    <Modal
      visible={!!logId}
      animationType="slide"
      transparent
      onRequestClose={handleSkip}
    >
      <KeyboardAvoidingView
        behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
        style={styles.overlay}
      >
        <TouchableOpacity
          style={styles.overlayTouchable}
          activeOpacity={1}
          onPress={handleSkip}
        />
        <View style={styles.card}>
          <Text style={styles.title}>Add a note?</Text>
          <Text style={styles.subtitle}>Optional — tap Skip to continue without a note</Text>
          <TextInput
            style={styles.input}
            value={note}
            onChangeText={setNote}
            placeholder="What were you thinking..."
            placeholderTextColor={colors.textSecondary}
            multiline
            maxLength={280}
            autoFocus
            textAlignVertical="top"
          />
          <View style={styles.buttonRow}>
            <TouchableOpacity style={styles.skipButton} onPress={handleSkip}>
              <Text style={styles.skipText}>Skip</Text>
            </TouchableOpacity>
            <TouchableOpacity
              style={[styles.saveButton, !note.trim() && styles.saveButtonDisabled]}
              onPress={handleSave}
              disabled={!note.trim()}
            >
              <Text style={styles.saveText}>Save Note</Text>
            </TouchableOpacity>
          </View>
        </View>
      </KeyboardAvoidingView>
    </Modal>
  );
}

const styles = StyleSheet.create({
  overlay: {
    flex: 1,
    backgroundColor: 'rgba(0,0,0,0.6)',
    justifyContent: 'flex-end',
  },
  overlayTouchable: {
    flex: 1,
  },
  card: {
    backgroundColor: colors.surface,
    borderTopLeftRadius: 24,
    borderTopRightRadius: 24,
    padding: spacing.xl,
    paddingBottom: spacing.xxxl,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: -4 },
    shadowOpacity: 0.1,
    shadowRadius: 12,
    elevation: 20,
  },
  title: {
    fontFamily: typography.fontFamily.bold,
    fontSize: typography.sizes.xl,
    color: colors.textPrimary,
    textAlign: 'center',
    marginBottom: spacing.xs,
  },
  subtitle: {
    fontFamily: typography.fontFamily.regular,
    fontSize: typography.sizes.sm,
    color: colors.textMuted,
    textAlign: 'center',
    marginBottom: spacing.lg,
  },
  input: {
    backgroundColor: colors.surfaceLight,
    borderRadius: 8,
    padding: spacing.md,
    color: colors.textPrimary,
    fontFamily: typography.fontFamily.regular,
    fontSize: typography.sizes.md,
    borderWidth: 1,
    borderColor: colors.border,
    marginBottom: spacing.lg,
    minHeight: 80,
  },
  buttonRow: {
    flexDirection: 'row',
    gap: spacing.md,
  },
  skipButton: {
    flex: 1,
    padding: spacing.md,
    borderRadius: 8,
    alignItems: 'center',
    backgroundColor: colors.surfaceLight,
  },
  skipText: {
    fontFamily: typography.fontFamily.medium,
    color: colors.textSecondary,
    fontSize: typography.sizes.md,
  },
  saveButton: {
    flex: 1,
    padding: spacing.md,
    borderRadius: 8,
    alignItems: 'center',
    backgroundColor: colors.accent,
  },
  saveButtonDisabled: {
    opacity: 0.5,
  },
  saveText: {
    fontFamily: typography.fontFamily.bold,
    color: '#000',
    fontSize: typography.sizes.md,
  },
});
```

**File 2: src/components/joystick/Joystick.tsx**

Three changes:

1. Import NoteEntryModal at the top (after existing imports):
```typescript
import { NoteEntryModal } from './NoteEntryModal';
```

2. Destructure `pendingLogId` and `clearPendingLogId` from useSwipeLog (line 87, currently `const { handleSwipe } = useSwipeLog(pillarId);`):
```typescript
const { handleSwipe, pendingLogId, clearPendingLogId } = useSwipeLog(pillarId);
```

3. Add `<NoteEntryModal>` at the end of the JSX return, just before the closing `</View>` of the wrapper (after the `labelContainer` View, before the final `</View>`):
```tsx
<NoteEntryModal logId={pendingLogId} onClose={clearPendingLogId} />
```

**File 3: src/components/joystick/index.ts**

Add NoteEntryModal to exports. Add this line:
```typescript
export { NoteEntryModal } from './NoteEntryModal';
```
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/life && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/joystick/NoteEntryModal.tsx exists and is >60 lines
    - src/components/joystick/NoteEntryModal.tsx contains `export function NoteEntryModal`
    - src/components/joystick/NoteEntryModal.tsx contains `<Modal visible={!!logId}`
    - src/components/joystick/NoteEntryModal.tsx contains `<KeyboardAvoidingView`
    - src/components/joystick/NoteEntryModal.tsx contains `updateLogNote(logId, note.trim())`
    - src/components/joystick/NoteEntryModal.tsx contains `maxLength={280}`
    - src/components/joystick/NoteEntryModal.tsx contains `handleSkip` and `handleSave`
    - src/components/joystick/NoteEntryModal.tsx contains `onRequestClose={handleSkip}` (Android back button dismisses)
    - src/components/joystick/NoteEntryModal.tsx contains `overlayTouchable` (tapping overlay dismisses)
    - src/components/joystick/Joystick.tsx contains `import { NoteEntryModal }`
    - src/components/joystick/Joystick.tsx contains `const { handleSwipe, pendingLogId, clearPendingLogId } = useSwipeLog(pillarId)`
    - src/components/joystick/Joystick.tsx contains `<NoteEntryModal logId={pendingLogId} onClose={clearPendingLogId} />`
    - src/components/joystick/index.ts contains `export { NoteEntryModal }`
    - `npx tsc --noEmit` exits with 0 (no type errors)
  </acceptance_criteria>
  <done>NoteEntryModal bottom-sheet modal exists with TextInput, Skip/Save buttons, overlay dismiss, and Android back button support. It is rendered inside every Joystick component, triggered by pendingLogId from useSwipeLog. The full note entry flow works: swipe -> log created -> modal appears -> user types note -> Save calls updateLogNote -> modal closes. Skip path: swipe -> modal appears -> Skip -> modal closes without saving note.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` exits 0
2. Test stubs exist: `test -f src/components/joystick/NoteEntryModal.test.tsx && test -f src/components/ui/LogHistoryItem.test.tsx`
3. Grep for key wiring: `grep -r "pendingLogId" src/components/joystick/` shows useSwipeLog.ts, Joystick.tsx, NoteEntryModal.tsx
4. Grep for return type: `grep "Promise<string>" src/stores/logStore.ts` confirms addLog returns string
5. Grep for updateLogNote usage: `grep "updateLogNote" src/components/joystick/NoteEntryModal.tsx` confirms note save path
</verification>

<success_criteria>
- Wave 0 test stubs exist for NoteEntryModal.test.tsx and LogHistoryItem.test.tsx
- addLog returns Promise<string> with the generated UUID
- useSwipeLog exposes pendingLogId (string | null) and clearPendingLogId callback
- NoteEntryModal appears as a bottom-sheet after every swipe (both quick and hold)
- Modal has TextInput (multiline, maxLength 280), Skip button, Save button (disabled when empty)
- Three dismiss paths: Skip button, overlay tap, Android back button
- Save calls updateLogNote(logId, noteText) then closes modal
- Skip closes modal without calling updateLogNote
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-swipe-note-entry/06-01-SUMMARY.md`
</output>
