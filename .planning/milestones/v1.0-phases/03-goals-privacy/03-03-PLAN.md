---
phase: 03-goals-privacy
plan: 3
wave: 2
depends_on: ["1", "2"]
files_modified:
  - src/components/goals/TargetFormModal.tsx
  - app/(tabs)/goals.tsx
autonomous: true
---

# Plan 3.3: Target Form & Creation

## Objective
Build the creation flow for Goals, allowing users to attach to a pillar, name the target, and easily mask it behind a codename inline.

## Context
- src/stores/targetStore.ts
- src/constants/codenames.ts

## Tasks

<task type="auto">
  <name>Build Target Form Modal</name>
  <files>
    src/components/goals/TargetFormModal.tsx
  </files>
  <action>
    Create a standalone React Native `Modal` UI overlay containing:
    - Input: Real Name (`TextInput` element)
    - Pillar Selection mapping exactly to `Pillars` constants (Afterlife/Self/Others tabs or segmented).
    - Toggle `Switch`: "Hide with Privacy Codename?"
    - Conditional Label: Automatically calling `getRandomCodename()` immediately underneath when toggled.
    - CTA button "Save".
    Submit logic dispatches directly to: `useTargetStore.getState().addTarget(pillarId, realName, codename)`.
  </action>
  <verify>npx tsc --noEmit src/components/goals/TargetFormModal.tsx</verify>
  <done>TargetFormModal UI interacts gracefully maintaining native component styles and updating state payload properties accurately.</done>
</task>

<task type="auto">
  <name>Integrate FAB to Open Modal</name>
  <files>
    app/(tabs)/goals.tsx
  </files>
  <action>
    Wire the FAB in `goals.tsx` to toggle the boolean local `visible` state of the `TargetFormModal` sub-component.
    Ensure state propagation thoroughly cleans inputs out when the user dismisses without saving to prevent cross-contamination.
  </action>
  <verify>npx tsc --noEmit app/(tabs)/goals.tsx</verify>
  <done>Pressing FAB controls Target creation modal, allowing seamless end-to-end SQLite goal insertion flows natively.</done>
</task>

## Success Criteria
- [ ] Users can map new Goals specifically to the constrained 3 Pillars safely.
- [ ] Random codenames correctly allocate alongside `isMasked` property flags globally.
- [ ] Adding targets updates the `<TargetCard>` views reactively out-of-the-box.
